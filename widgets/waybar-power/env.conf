# ponytail: trivial single-var file — could hardcode in install.sh
# Install destination for the waybar-power unit.
# Override by exporting INSTALL_ROOT before running install.sh.
INSTALL_ROOT="${INSTALL_ROOT:-$HOME/.config/acw/waybar-power}"
