# Tasks: migrate-custom-units

- [x] Factory bootstrap: `factory/` manifest schema + `factory/validate` (bash).
- [x] Unit `wallpaper` (service): wallhaven-wallpaper + selector + auto-theme systemd path/service + wallchange-color; keybinds documented.
- [x] Unit `scheme-rotator` (widget): caelestia-auto-theme.sh + state file location.
- [x] Unit `waybar-todo` (widget): todo.sh + todo_tui.sh + state/config files.
- [x] Unit `waybar-countdown` (widget): countdown.sh + TUI + state.
- [x] Unit `vpn-surfshark` (widget + service): shell.json delta + systemd units (sudo) + nftables config.
- [x] Unit `waybar-power` (widget): lock.sh + power.sh.
- [x] Unit `waybar-theme` (widget): theme.sh + styles contract.
- [x] Unit `clipboard-rofi` (widget): clipboard-rofi.sh + keybind.
- [x] Unit `quickshell-watchdog` (service): watchdog.sh + timer; fix missing WantedBy.
- [x] Unit `opencode-stats` (service): fetch script + timer.
- [x] Docs: update repo README with unit index.
